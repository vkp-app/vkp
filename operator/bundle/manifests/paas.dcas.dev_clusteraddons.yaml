apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: clusteraddons.paas.dcas.dev
spec:
  group: paas.dcas.dev
  names:
    kind: ClusterAddon
    listKind: ClusterAddonList
    plural: clusteraddons
    singular: clusteraddon
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterAddon is the Schema for the clusteraddons API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterAddonSpec defines the desired state of ClusterAddon
            properties:
              description:
                description: Description is the human-readable description of the
                  addon shown in the addon marketplace.
                type: string
              displayName:
                description: DisplayName is the human-readable name of the addon shown
                  in the addon marketplace.
                type: string
              logo:
                description: Logo is the URL of an image that should be shown in the
                  addon marketplace for this addon.
                type: string
              maintainer:
                description: Maintainer is the name/contact information of the addon.
                type: string
              resources:
                items:
                  properties:
                    configMap:
                      description: ConfigMap is a v1.ConfigMap that contains a number
                        of Kustomize files. Mutually-exclusive with URL and Secret.
                      properties:
                        name:
                          description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            TODO: Add other useful fields. apiVersion, kind, uid?'
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    oci:
                      description: OCI is an OCI-compliant container image that contains
                        a Kustomize directory.
                      properties:
                        imagePullSecret:
                          description: LocalObjectReference contains enough information
                            to let you locate the referenced object inside the same
                            namespace.
                          properties:
                            name:
                              description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                TODO: Add other useful fields. apiVersion, kind, uid?'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        name:
                          type: string
                      type: object
                    secret:
                      description: Secret is a v1.Secret that contains a number of
                        Kustomize files. Mutually-exclusive with URL and ConfigMap.
                      properties:
                        name:
                          description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            TODO: Add other useful fields. apiVersion, kind, uid?'
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    url:
                      description: URL is a Kustomize-compatible HTTPS URL to a Kustomize
                        directory. Mutually-exclusive with ConfigMap and Secret.
                      type: string
                  type: object
                type: array
              source:
                description: Source indicates where the addon came from and how trustworthy
                  it should be considered.
                type: string
              sourceURL:
                description: SourceURL is an external HTTP address that can be used
                  by users to find more information about an addon.
                type: string
            required:
            - displayName
            - maintainer
            - resources
            type: object
          status:
            description: ClusterAddonStatus defines the observed state of ClusterAddon
            properties:
              resourceDigests:
                additionalProperties:
                  type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
