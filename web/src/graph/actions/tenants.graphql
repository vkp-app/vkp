query tenants {
    tenants {
        name
        owner
        status {
            phase
        }
    }
}

query tenant($name: ID!) {
    tenant(name: $name) {
        name
        owner
        status {
            phase
        }
    }
}
