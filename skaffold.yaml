apiVersion: skaffold/v3
kind: Config
metadata:
  name: local
manifests:
  kustomize:
    paths:
      - local/idp
      - local/prometheus
    buildArgs:
      - --enable-helm
deploy:
  kubeContext: minikube
